# Calculator

A simple calculator written in Zig.

## Features

- Basic arithmetic (addition)
- CLI interface

## Requirements

- [Zig](https://ziglang.org/) compiler

## Usage

Build the project:

```sh
zig build
```

Run the CLI:

```sh
zig build run
```

## Testing

```sh
zig build test
```
